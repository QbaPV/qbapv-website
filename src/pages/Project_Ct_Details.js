const Project_Ct_Details = ({ project }) => {
    return (
        <div>
            <h1>Detalles del proyecto: {project}</h1>
            {/* Agrega más detalles aquí */}
        </div>
    );
};

export default Project_Ct_Details;